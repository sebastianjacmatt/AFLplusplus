{
  "_comment": "GRPO v3_3 — Three targeted fixes for validity collapse. (1) mask_count 3→1: removes the 3-simultaneous-span constraint; expected valid_rate floor ~47% vs ~10.5%, near-zero all-invalid groups, balanced gradient signal. (2) ref_update_every 999: reference permanently frozen at zero-init LoRA (= base model); KL always pulls toward base model direction = valid syntax, collapsed-reference trap eliminated. (3) lr 2e-5→1e-4 reverted: higher LR needed for adapter to leverage sparse valid-group gradients and escape attractor states. kl_coef 0.05→0.10: safe to increase now that reference is healthy; clip_epsilon 0.2→0.1: actual fix for high clip fraction without LR reduction.",

  "model_name_or_path": "Salesforce/codet5p-220m",
  "device":             "cuda",
  "max_length":         512,
  "mask_probability":   0.15,
  "mean_span_length":   3,
  "min_span_length":    1,
  "max_span_length":    5,
  "top_k":              32,
  "top_p":              0.95,
  "temperature":        1.0,
  "max_new_tokens_per_mask": 5,

  "fuzz_count":      512,
  "mask_count":      1,
  "mask_strategy":   "span",
  "bitmap_size":     131072,
  "idf_alpha":       0.6,
  "finetune_every":  16,
  "validity_bonus":  0.3,

  "algorithm":          "grpo",
  "learning_rate":      2e-5,
  "warmup_ratio":       0.05,
  "bf16":               true,
  "train_batch_size":   32,
  "num_train_epochs":   1,
  "kl_coef":            0.05,
  "clip_epsilon":       0.2,
  "ref_update_every":   999,

  "lora_r":              8,
  "lora_alpha":          16,
  "lora_dropout":        0.1,
  "lora_target_modules": "q,v",

  "group_size":   16,
  "norm_epsilon": 1e-8,

  "enable_logging": true,
  "logging_steps":  1
}
