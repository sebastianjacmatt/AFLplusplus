{
  "_comment": "GRPO v2 — v2_4 update: warmup_ratio reverted 0.15→0.05 (0.15 amplified outlier updates at high valid_rate collapse by keeping peak LR for the epoch-end sparse valid samples; v2_3 data: 3 CRITICAL clip epochs, KL min -2.856 vs -0.771). Pre-filter added: zero-variance groups (max-min reward < 0.01) dropped before batching, replacing the post-hoc active-mask in grpo.py. Structural benefit: fewer wasted forward passes, cleaner batch density.",

  "model_name_or_path": "Salesforce/codet5p-220m",
  "device":             "cuda",
  "max_length":         512,
  "mask_probability":   0.15,
  "mean_span_length":   3,
  "min_span_length":    1,
  "max_span_length":    5,
  "top_k":              32,
  "top_p":              0.95,
  "temperature":        1.0,
  "max_new_tokens_per_mask": 5,

  "fuzz_count":      512,
  "mask_strategy":   "span",
  "bitmap_size":     131072,
  "idf_alpha":       0.6,
  "finetune_every":  16,
  "validity_bonus":  0.3,

  "algorithm":        "grpo",
  "learning_rate":    1e-4,
  "warmup_ratio":     0.05,
  "bf16":             true,
  "train_batch_size": 32,
  "num_train_epochs": 1,
  "kl_coef":          0.005,
  "clip_epsilon":     0.2,

  "lora_r":              8,
  "lora_alpha":          16,
  "lora_dropout":        0.1,
  "lora_target_modules": "q,v",

  "group_size":   16,
  "norm_epsilon": 1e-8,

  "enable_logging": true,
  "logging_steps":  1
}
