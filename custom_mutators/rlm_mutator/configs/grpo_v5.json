{
  "_comment": "GRPO v5 — SFT adapter warmup pipeline. Inherits all v4 settings. Key change: sft_adapter_path points to a LoRA checkpoint produced by scripts/sft_warmup.py; the adapter is loaded into BaseTrainer.__init__ before snapshot_ref(), so pi_ref is anchored to the SFT checkpoint rather than zero-init LoRA. SFT corpus now points to the cleaned+deduped corpus (51k files) built by scripts/build_corpus.py. sft_warmup_steps kept at 0 — in-process warmup is retired; use scripts/sft_warmup.py instead.",

  "model_name_or_path": "Salesforce/codet5p-220m",
  "device":             "cuda",
  "max_length":         512,
  "mask_probability":   0.15,
  "mean_span_length":   3,
  "min_span_length":    1,
  "max_span_length":    5,
  "top_k":              32,
  "top_p":              0.95,
  "temperature":        1.0,
  "max_new_tokens_per_mask": 5,

  "fuzz_count":      512,
  "mask_strategy":   "span",
  "bitmap_size":     131072,
  "idf_alpha":       0.6,
  "finetune_every":  16,
  "validity_bonus":  0.3,

  "algorithm":          "grpo",
  "learning_rate":      2e-5,
  "warmup_ratio":       0.05,
  "bf16":               true,
  "train_batch_size":   32,
  "num_train_epochs":   1,
  "kl_coef":            0.05,
  "clip_epsilon":       0.2,
  "ref_update_every":   3,
  "mlm_coef":           0.1,

  "sft_corpus_path":    "/home/sebastian/Documents/data_store/dataset/corpus/train",
  "sft_warmup_steps":   0,
  "sft_adapter_path":   "",

  "lora_r":              8,
  "lora_alpha":          16,
  "lora_dropout":        0.1,
  "lora_target_modules": "q,v",

  "group_size":      16,
  "norm_epsilon":    1e-8,
  "advantage_clip":  3.0,

  "enable_logging": true,
  "logging_steps":  1
}
