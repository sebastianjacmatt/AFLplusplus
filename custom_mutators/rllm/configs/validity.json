{
    "_comment": "Validity-first experiment. queue_new_entry rejects every non-valid corpus find, so the LLM only ever sees syntactically valid JS parents. Hypothesis: with small masks (span=1, rate=0.03) the model converges toward high steady-state validity as the corpus grows. Expected tradeoff: coverage growth slows because invalid inputs (which exercise parse-stage paths) are excluded. Run with -c configs/validity.json.",

    "fuzz_count": 16,
    "finetune_every": 100,

    "model_name_or_path": "Salesforce/codet5p-220m",
    "sampling_method": "contrastive",
    "max_new_tokens": 64,
    "device": "auto",

    "temperature": 1.0,
    "top_p": 0.95,
    "top_k": 50,
    "no_repeat_ngram_size": 3,

    "penalty_alpha": 0.6,
    "contrastive_top_k": 4,

    "corruption_rate": 0.03,
    "mean_span_length": 1.0,
    "min_span_length": 1,
    "max_span_length": 1,
    "whole_word_masking": true
}
