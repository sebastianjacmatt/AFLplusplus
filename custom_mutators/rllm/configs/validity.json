{
    "_comment": "Historical preset. The queue_new_entry-based validity filter it documented was removed when we switched to the u16 queue (Option B) — under that architecture, queue drift can't happen, so the filter is no longer needed. The masking parameters here happen to match the post-switch defaults (single-token Overwrite), so it remains a usable preset; the comment is retained as design history.",

    "fuzz_count": 16,
    "finetune_every": 100,

    "model_name_or_path": "Salesforce/codet5p-220m",
    "sampling_method": "contrastive",
    "max_new_tokens": 64,
    "device": "auto",

    "temperature": 1.0,
    "top_p": 0.95,
    "top_k": 50,
    "no_repeat_ngram_size": 3,

    "penalty_alpha": 0.6,
    "contrastive_top_k": 4,

    "corruption_rate": 0.03,
    "mean_span_length": 1.0,
    "min_span_length": 1,
    "max_span_length": 1,
    "whole_word_masking": false
}
